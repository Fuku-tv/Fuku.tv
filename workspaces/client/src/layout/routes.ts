import type { RouteProps } from 'react-router-dom';

const routes: RouteProps[] = [
  { path: '/' },
  { path: '/about' },
  { path: '/profile' },
  { path: '/leaderboards' },
  { path: '/prizes' },
  { path: '/store' },
  { path: '/claw-customization' },
];

export default routes;
